const menuButton=document.querySelector('.menu-button');const drawer=document.querySelector('.mobile-drawer');if(menuButton&&drawer){const setOpen=o=>{drawer.classList.toggle('open',o);drawer.setAttribute('aria-hidden',String(!o));menuButton.setAttribute('aria-expanded',String(o));};menuButton.addEventListener('click',()=>setOpen(!drawer.classList.contains('open')));drawer.addEventListener('click',e=>{if(e.target instanceof HTMLAnchorElement)setOpen(false);});}document.querySelectorAll('.nav-item').forEach(item=>{item.addEventListener('click',e=>{if(matchMedia('(hover: none)').matches&&e.target.closest('.nav-item>a')){e.preventDefault();item.classList.toggle('open');}});});